/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    'tailwindcss': {},
    'tailwindcss/nesting': {},
    'autoprefixer': {},
    'postcss-import': {},
  },
};

export default config;
